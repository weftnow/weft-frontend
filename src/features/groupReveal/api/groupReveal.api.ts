import { groupRevealSchema, type GroupReveal } from "../schemas/groupReveal.schema";
export interface GroupRevealClient { load(formToken: string): Promise<{ status: "waiting" } | { status: "ready"; group: GroupReveal }>; confirm(formToken: string): Promise<void>; }
export const groupRevealClient: GroupRevealClient = { async load(formToken) { const r = await fetch(`/api/questionnaire/${encodeURIComponent(formToken)}/group`, { signal: AbortSignal.timeout(8_000) }); if (r.status === 204) return { status: "waiting" }; if (!r.ok) throw new Error("group unavailable"); return { status: "ready", group: groupRevealSchema.parse(await r.json()) }; }, async confirm(formToken) { const r = await fetch(`/api/questionnaire/${encodeURIComponent(formToken)}/group/confirm`, { method: "POST", signal: AbortSignal.timeout(8_000) }); if (!r.ok) throw new Error("confirmation unavailable"); } };
